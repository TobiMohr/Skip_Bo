package de.htwg.se.Skip_Bo.model

import org.scalatest.matchers.should.Matchers
import org.scalatest.wordspec.AnyWordSpec

class StackSpec extends AnyWordSpec with Matchers{
  "A Stack" when {
//
//    "new" should {
//      val mtStack = new Stack(Nil)
//      val redStack = new Stack(List(Card(Value.One), Card(Value.Two)))
//      val blueStack =  new Stack(List(Card(Value.Three), Card(Value.Joker)))
//    }
//
//      "should have a top Card" in {
//        redStack.topCard() should be("1")
//        blueStack.topCard() should be("3")
//        mtStack.topCard() should be("Keine Karte im Stapel")
//      }
//      "or be empty" in {
//        mtStack.empty() should be(true)
//        redStack.empty() should be(false)
//        blueStack.empty() should be(false)
//      }
//      "be able to print what it contains" in {
//        redStack.contains() should be("1, 2")
//      }
//    }
}
